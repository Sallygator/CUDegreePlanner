<div class="col-md">
    <input type="text" placeholder="Search..." id="course_search" onsubmit="searchForCourse(input)"> 
    <ul id="course_search_results" class="connectedSortable">
        {{#each all_courses}}
        {{course_card}}
        {{/each}}
    </ul>
</div>